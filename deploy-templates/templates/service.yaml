apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-maven-java21-branch-names.fullname" . }}
  labels:
    {{- include "java-maven-java21-branch-names.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "java-maven-java21-branch-names.selectorLabels" . | nindent 4 }}
